<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ViewPageAndSlider" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="Background">@color/BackLight</item>
        <item name="Button">@color/ButtomLight</item>
    </style >


    <style name="Theme.ViewPageAndSlider" parent="Base.Theme.ViewPageAndSlider" />
</resources>